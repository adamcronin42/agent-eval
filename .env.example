# REQUIRED: Model Configuration
MODEL_NAME=gemini/gemini-2.5-flash  # litellm model name (gemini/gemini-2.5-flash, gpt-4o-mini, claude-3-haiku-20240307, etc)

# Provider-specific API Keys (set the one matching your MODEL_NAME)
GEMINI_API_KEY=your-google-ai-studio-key     # For gemini/* models
OPENAI_API_KEY=your-openai-key               # For gpt-* models
ANTHROPIC_API_KEY=your-anthropic-key         # For claude-* models

# OPTIONAL: Override Defaults
MAX_ITERATIONS=10       # Max loop iterations (default: 10)
MAX_RETRIES=3          # API retry attempts (default: 3)
TIMEOUT_SECONDS=30     # Per API call timeout (default: 30)

# Evaluation Settings
JUDGE_MODEL=gemini/gemini-2.5-flash      # Model for LLM judge (can differ from agent, uses same API keys above)
AUTO_APPROVE_TOOLS=false                 # Auto-approve tool calls (set true for eval)